import { test } from '@playwright/test';

test.describe.skip('Child session visual coverage', () => {
  test('TODO: add a safe dev-only child QA login helper before enabling child-session tests', () => {
    // TODO: enable this once the repo has a safe child-only QA login fixture/helper.
  });
});
